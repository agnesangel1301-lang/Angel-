<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Simple Blog with Comments</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9fafb;
      margin: 0;
      padding: 0;
    }
    header {
      background: #1f2937;
      color: white;
      padding: 20px;
      text-align: center;
    }
    .container {
      max-width: 800px;
      margin: 20px auto;
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1, h2 {
      color: #111827;
    }
    p {
      line-height: 1.6;
      color: #374151;
    }
    .comment-section {
      margin-top: 30px;
    }
    .comment-form textarea {
      width: 100%;
      height: 80px;
      padding: 10px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      font-size: 14px;
    }
    .comment-form button {
      margin-top: 10px;
      padding: 10px 20px;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }
    .comment-form button:hover {
      background: #1d4ed8;
    }
    .comment-list {
      margin-top: 20px;
    }
    .comment {
      background: #f3f4f6;
      padding: 10px 15px;
      border-radius: 6px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>

  <header>
    <h1>My Simple Blog</h1>
  </header>

  <div class="container">
    <article>
      <h2>Welcome to My First Blog Post</h2>
      <p>
        This is a simple example of a blog page made with plain HTML, CSS, and JavaScript. 
        You can add your thoughts below in the comment section. 
        In a real app, these comments would be saved to a server or database.
      </p>
    </article>

    <section class="comment-section">
      <h3>Comments</h3>
      <div class="comment-list" id="commentList">
        <!-- Comments will appear here -->
      </div>

      <div class="comment-form">
        <textarea id="commentInput" placeholder="Write your comment..."></textarea>
        <button id="addCommentBtn">Post Comment</button>
      </div>
    </section>
  </div>

  <script>
    const commentList = document.getElementById("commentList");
    const commentInput = document.getElementById("commentInput");
    const addCommentBtn = document.getElementById("addCommentBtn");

    let comments = [];

    function renderComments() {
      commentList.innerHTML = "";
      comments.forEach((text, index) => {
        const div = document.createElement("div");
        div.className = "comment";
        div.textContent = text;
        commentList.appendChild(div);
      });
    }

    addCommentBtn.addEventListener("click", () => {
      const text = commentInput.value.trim();
      if (text === "") {
        alert("Please write a comment before posting.");
        return;
      }
      comments.push(text);
      commentInput.value = "";
      renderComments();
    });

    // Optional: Save comments in localStorage
    window.addEventListener("beforeunload", () => {
      localStorage.setItem("comments", JSON.stringify(comments));
    });

    window.addEventListener("load", () => {
      const saved = JSON.parse(localStorage.getItem("comments"));
      if (saved) {
        comments = saved;
        renderComments();
      }
    });
  </script>
</body>
</html>